---
layout: default
---
<div class="wrapper page page--article page--wide">
  {%- if title -%}
  <h1 class="page__title" data-pagefind-meta="title">{{ title }}</h1>
  {%- endif -%}
  <div class="prose">
    {{ content }}
  </div>
</div>
